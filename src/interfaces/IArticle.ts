export interface IArticle {
  // интерфейс для статей в массиве статей
  id: number;
  tag: string;
  title: string;
  author: string;
  createdAt: number;
  image: string;
  readTime: number;
  description: string;
}
